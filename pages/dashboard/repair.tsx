import MenuLayout from '../../components/MenuLayout/MenuLayout';
import { Typography } from '@mui/material';

const Repair = () => {
  return (
    <MenuLayout>
      <Typography variant="body1">Taller</Typography>
    </MenuLayout>
  );
};

export default Repair;
