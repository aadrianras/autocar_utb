import MenuLayout from '../../components/MenuLayout/MenuLayout';
import { Typography } from '@mui/material';

const HumanResource = () => {
  return (
    <MenuLayout>
      <Typography variant="body1">Recursos humanos</Typography>
    </MenuLayout>
  );
};

export default HumanResource;
